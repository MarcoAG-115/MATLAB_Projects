function  av  = AVG( x, num )
%This function will find the average of a vector

%Inputs: x (the vector) & num (the number of elements in the vector)
%Output: av (the average)

av = sum(x)/num;


end

